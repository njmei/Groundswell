% add toolbox to path
addpath(genpath('~/packages/taylor_matlab_toolbox/trunk'));
% % add local toolbox
% addpath(sprintf('%s/toolbox',pwd));

% % get path to parent dir
% pwd_save=pwd;
% cd ..
% parent=pwd;
% cd(pwd_save);

% % add toolbox dirs that live underneath the parent dir
% addpath(sprintf('%s/toolbox',parent));
% addpath(sprintf('%s/toolbox/fourier_stuff',parent));
% addpath(sprintf('%s/toolbox/hds',parent));
% addpath(sprintf('%s/toolbox/measure_spont',parent));
